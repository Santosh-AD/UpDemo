enum QType {
  radio, //Radio Button having only Yes and No
  checkOpt4, //Checkbox option having 4 options
  checkOpt3, //Checkbox Option having 3 options
  select4, //Multiselect options having 4 options
  yesNo,
}
